import { Breakpoints } from '../type';

export const breakpoints: Breakpoints['breakpoints'] = {
  xs: 0, // phone
  sm: 600, // tablet
  md: 900, // small laptop
  lg: 1200, // desktop
  xl: 1536, // large screen
};
